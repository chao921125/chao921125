<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.gok.dao.UserDao">

    <resultMap id="resultUser" type="cn.gok.entity.User">
        <result property="user_id" column="user_id"/>
        <result property="user_name" column="user_name"/>
        <result property="user_pwd" column="user_pwd"/>
        <result property="user_tel" column="user_tel"/>
        <result property="user_email" column="user_emai"/>
    </resultMap>
    <!--增加新用户-->
    <insert id="addUser" parameterType="cn.gok.entity.User">
        insert into user values (#{user_id},#{user_name},#{user_pwd},#{user_tel},#{user_email},#{user_role})
    </insert>


    <!--查询用户信息-->
    <select id="selectUser" resultMap="resultUser">
        select * from user;
    </select>

    <!--删除用户-->
    <delete id="deleteUser" parameterType="int">
        delete from user where id = #{user_id}
    </delete>

    <!--修改用户-->
    <update id="updateUser">
        update user
        <trim prefix="set" suffixOverrides=",">
            <if test="user_name!=null and user_name!='' ">
            user_name = #{user_name},
            </if>
            <if test="user_pwd!=null and user_pwd!='' ">
                user_pwd = #{user_pwd},
            </if>
            <if test="user_tel!=null and user_tel!='' ">
                user_tel = #{user_tel},
            </if>
            <if test="user_email!=null and user_email!='' ">
                user_email = #{user_email},
            </if>
        </trim>
        where user_id=#{user_id}
    </update>
</mapper>